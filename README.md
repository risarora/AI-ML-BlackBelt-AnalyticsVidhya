# AI-ML-BlackBelt-AnalyticsVidhya


How do we decide the pre-processing step while using different pre-trained models?

The pre-processing step is defined based on the pre-trained model that we are using
